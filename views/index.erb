<h1>Chitter</h1>
<div id="peeps-feed">
  <% @peeps.each do |peep| %>
    <p><%= peep["message"] %></p>
  <% end %>
</div>

<div id="add-another-peep"> 
  <form method="GET" action="/add_peep">
    <input type="submit" value="Add Peep">
  </form>
</div>