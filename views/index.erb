<!DOCTYPE html>
<html>
<head>
	<title>Chitter</title>
	<style type="text/css">
		body {
			width: 90vw; 
			height: 100vh;
			text-align: center;
			background: radial-gradient(ellipse at top, #e66465, transparent),
            radial-gradient(ellipse at bottom, #4d9f0c, transparent);
		}
		ul {
			list-style-type: none;
			margin: 10vh auto;
		}
		li {
			text-align: left;
			width: 50vw;
			margin: 10px auto;
			padding: 10px 0;
			border-top: 1px solid white;
		}
		h1 {
			color: white;
		}
	</style>
</head>
<body>
	<h1><s>Shitter</s>  Chitter</h1>
	<% if @peeps.length %>
	<ul>
		<% for @peep in @peeps %>
			<li>
				<p><%= @peep.message %></p>
				<p>Date posted: <%= @peep.date %></p>
			</li>
		<% end %>
	</ul>
	<% end %>
</body>
</html>
