<head> 
  <link href="<%= url("/stylesheets/app.css") %>" rel="stylesheet" type="text/css" />
  <ul>
  <% @peeps.each do |peep| %>
    <li>
      <%= peep.message %>
        <%= peep.pretty_date %>
    </li>
  <% end %>
</ul>